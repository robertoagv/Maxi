@model IEnumerable<WebMaxiFarmacia.Models.Sale>

@{
    ViewBag.Title = "rangoFecha";
}

<h2>rangoFecha</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@

@using (Ajax.BeginForm("rangoFecha", new AjaxOptions
{
    HttpMethod = "POST",
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "buscarid"
}))
{

    <div class="form-group form-inline">
        <div>
            <input type="date" name="d" id="d" class="form-control" required placeholder="Buscar" autofocus autocomplete="on" />
            <b>-</b>
            <input type="date" name="hasta" id="hasta" class="form-control" required placeholder="Buscar" autofocus autocomplete="on" />
            <input type="submit" value="Buscar" class="btn btn-primary" />
        </div>
    </div>
}


<table class="table table-bordered table-hover table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fechavta)
        </th>
        <th>
           Codigo Barra
        </th>
        <th>
            Producto
        </th>
        <th>
            Precio
        </th>
        <th>
            Cantidad
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombrecte)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fechavta)
        </td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombrecte)
        </td>
        <td>@Html.ActionLink("Details", "Details", new { id = item.SaleID })</td>
        @*<td>
                @Html.ActionLink("Edit", "Edit", new { id=item.SaleID }) |
                @Html.ActionLink("Details", "Details", new { id=item.SaleID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.SaleID })
            </td>*@
    </tr>
    <tr>
        <td></td>
        <td><b>Cantidad Vendida</b></td>
        <td></td>
        <td><b>Total Ventas</b></td>
        <td></td>
        <td></td>
        <td></td>
    </tr>
}

</table>
